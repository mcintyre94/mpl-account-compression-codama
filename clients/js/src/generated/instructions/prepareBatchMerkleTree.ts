/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/kit';
import { MPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const PREPARE_BATCH_MERKLE_TREE_DISCRIMINATOR = new Uint8Array([
  230, 124, 120, 196, 249, 134, 199, 128,
]);

export function getPrepareBatchMerkleTreeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PREPARE_BATCH_MERKLE_TREE_DISCRIMINATOR
  );
}

export type PrepareBatchMerkleTreeInstruction<
  TProgram extends string = typeof MPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMerkleTree extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountNoop extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerkleTree extends string
        ? WritableAccount<TAccountMerkleTree>
        : TAccountMerkleTree,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountNoop extends string
        ? ReadonlyAccount<TAccountNoop>
        : TAccountNoop,
      ...TRemainingAccounts,
    ]
  >;

export type PrepareBatchMerkleTreeInstructionData = {
  discriminator: ReadonlyUint8Array;
  maxDepth: number;
  maxBufferSize: number;
};

export type PrepareBatchMerkleTreeInstructionDataArgs = {
  maxDepth: number;
  maxBufferSize: number;
};

export function getPrepareBatchMerkleTreeInstructionDataEncoder(): FixedSizeEncoder<PrepareBatchMerkleTreeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', fixEncoderSize(getBytesEncoder(), 8)],
      ['maxDepth', getU32Encoder()],
      ['maxBufferSize', getU32Encoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: PREPARE_BATCH_MERKLE_TREE_DISCRIMINATOR,
    })
  );
}

export function getPrepareBatchMerkleTreeInstructionDataDecoder(): FixedSizeDecoder<PrepareBatchMerkleTreeInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
    ['maxDepth', getU32Decoder()],
    ['maxBufferSize', getU32Decoder()],
  ]);
}

export function getPrepareBatchMerkleTreeInstructionDataCodec(): FixedSizeCodec<
  PrepareBatchMerkleTreeInstructionDataArgs,
  PrepareBatchMerkleTreeInstructionData
> {
  return combineCodec(
    getPrepareBatchMerkleTreeInstructionDataEncoder(),
    getPrepareBatchMerkleTreeInstructionDataDecoder()
  );
}

export type PrepareBatchMerkleTreeInput<
  TAccountMerkleTree extends string = string,
  TAccountAuthority extends string = string,
  TAccountNoop extends string = string,
> = {
  merkleTree: Address<TAccountMerkleTree>;
  /**
   * Authority that controls write-access to the tree
   * Typically a program, e.g., the Bubblegum contract validates that leaves are valid NFTs.
   */
  authority: TransactionSigner<TAccountAuthority>;
  /** Program used to emit changelogs as cpi instruction data. */
  noop: Address<TAccountNoop>;
  maxDepth: PrepareBatchMerkleTreeInstructionDataArgs['maxDepth'];
  maxBufferSize: PrepareBatchMerkleTreeInstructionDataArgs['maxBufferSize'];
};

export function getPrepareBatchMerkleTreeInstruction<
  TAccountMerkleTree extends string,
  TAccountAuthority extends string,
  TAccountNoop extends string,
  TProgramAddress extends
    Address = typeof MPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
>(
  input: PrepareBatchMerkleTreeInput<
    TAccountMerkleTree,
    TAccountAuthority,
    TAccountNoop
  >,
  config?: { programAddress?: TProgramAddress }
): PrepareBatchMerkleTreeInstruction<
  TProgramAddress,
  TAccountMerkleTree,
  TAccountAuthority,
  TAccountNoop
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? MPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merkleTree: { value: input.merkleTree ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
    noop: { value: input.noop ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.merkleTree),
      getAccountMeta(accounts.authority),
      getAccountMeta(accounts.noop),
    ],
    programAddress,
    data: getPrepareBatchMerkleTreeInstructionDataEncoder().encode(
      args as PrepareBatchMerkleTreeInstructionDataArgs
    ),
  } as PrepareBatchMerkleTreeInstruction<
    TProgramAddress,
    TAccountMerkleTree,
    TAccountAuthority,
    TAccountNoop
  >;

  return instruction;
}

export type ParsedPrepareBatchMerkleTreeInstruction<
  TProgram extends string = typeof MPL_ACCOUNT_COMPRESSION_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merkleTree: TAccountMetas[0];
    /**
     * Authority that controls write-access to the tree
     * Typically a program, e.g., the Bubblegum contract validates that leaves are valid NFTs.
     */

    authority: TAccountMetas[1];
    /** Program used to emit changelogs as cpi instruction data. */
    noop: TAccountMetas[2];
  };
  data: PrepareBatchMerkleTreeInstructionData;
};

export function parsePrepareBatchMerkleTreeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedPrepareBatchMerkleTreeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      merkleTree: getNextAccount(),
      authority: getNextAccount(),
      noop: getNextAccount(),
    },
    data: getPrepareBatchMerkleTreeInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
